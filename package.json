{
  "name": "visual-comparison",
  "version": "1.0.0",
  "description": "Scalable visual regression testing suite for the Practice Software Testing 'Toolshop' app — Playwright + TypeScript. Records pixel baselines against a clean build and proves it catches UI regressions by diffing against an intentionally buggy build.",
  "type": "commonjs",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "playwright test",
    "test:quick": "playwright test --project=desktop-chromium --project=mobile-chromium",
    "test:smoke": "playwright test --grep @smoke",
    "test:visual": "playwright test --grep @visual",
    "test:bugs": "cross-env BASE_URL=https://with-bugs.practicesoftwaretesting.com playwright test",
    "baseline": "playwright test --update-snapshots=all",
    "baseline:changed": "playwright test --update-snapshots=changed",
    "report": "playwright show-report",
    "merge-report": "playwright merge-reports --reporter html ./all-blob-reports",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure": "npm run allure:generate && allure open allure-report",
    "typecheck": "tsc --noEmit",
    "test:scripts": "node --test \"scripts/**/*.test.mjs\"",
    "check:lockstep": "node scripts/check-playwright-version.mjs",
    "docker:baseline": "docker compose run --build --rm visual npm run baseline",
    "docker:test": "docker compose run --build --rm visual npx playwright test --update-snapshots=none",
    "docker:bugs": "docker compose run --build --rm -e BASE_URL=https://with-bugs.practicesoftwaretesting.com visual npx playwright test --update-snapshots=none"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/node": "24.13.3",
    "allure-commandline": "^2.43.0",
    "allure-playwright": "^3.12.0",
    "cross-env": "^10.1.0",
    "typescript": "7.0.2"
  }
}
